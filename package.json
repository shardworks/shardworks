{
  "name": "shardworks",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w packages/tq",
    "test": "npm run test --workspaces --if-present"
  }
}
